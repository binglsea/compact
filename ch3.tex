\documentclass[main.tex]{subfiles}
\begin{document}

	
\chapter{Works of Mack, Worrell, Liu and Smith} 
	\label{ch3}

\section{Works of Mack, Worrell}\label{ch3_mack_worrell}

{\lemma{} \label{le:3_1_1} 
	If every directed $($increasing$)$ open $\lambda$-cover of $X$ has a locally finite 
	open refinement, then every directed $($increasing$)$ open $\lambda$-cover has a 
	locally open shrink.
}
\begin{proof}
Suppose $\mathscr{U} = \{U_\alpha : \alpha < \lambda\}$ is a directed (increasing) open cover of $X$. By hypothesis, $\mathscr{U}$ has a locally finite open refinement $\mathscr{G} = \{G_\alpha : \alpha < \lambda\}$ with $G_\alpha \subset U_\alpha$. 
For $\alpha < \lambda$, let

$$W_\alpha = X \setminus \bigcup \{ \overline{G_\gamma} : \gamma < \lambda,  
G_\gamma \not\subset U_\alpha\}.$$
Then $\mathscr{W} = \{W_\alpha : \alpha < \lambda\}$ is an open cover of $X$ 
with $\overline{W_\alpha} \subset U_\alpha$. 
Since $\mathscr{U}$ is directed (increasing), $\mathscr{W}$ is also 
directed (increasing). Let $\mathscr{V} = \{V_\alpha : \alpha < \lambda\}$ be  a locally finite open refinement of $\mathscr{W}$ such that $V_\alpha \subset W_\alpha$. Then $\mathscr{V}$ is a locally finite open shrink of $\mathscr{U}$. 
\end{proof} 

{\theorem{} \label{lth:3_1_2} 
	\textup{(Mack [1967]\textsuperscript{\cite{Mack}})} For any $X$, the following are equivalent:

\textup{(i)} $X$ is $\lambda$-paracompact.

\textup{(ii)} Every directed open $\lambda$-cover of $X$ has a  locally finite open refinement.

\textup{(iii)} Every increasing open $\lambda$-cover of $X$ has a  locally finite open refinement.

\textup{(iv)}  Every increasing open $\lambda$-cover has a locally finite open shrink.

\textup{(v)}  Every increasing open $\lambda$-cover $\mathscr{U}$
has a $\sigma$-locally finite open refinement $\mathscr{V}$ such that 
$\overline{\mathscr{V}}$ refines $\mathscr{U}$.
}

\begin{proof}
It is clear that (i) $\Rightarrow$ (ii) $\Rightarrow$ (iii) and (iv) $\Rightarrow$ (v).

(iii) $\Rightarrow$ (iv). Lemma \ref{le:1_2_3}.

(v) $\Rightarrow$ (i). From Theorem \ref{th:8_2_18}. % TO_DO 8.2.18
\end{proof} 

{\theorem{}  \label{th:3_1_3} 
For any $X$ the following are equivalent:

(i) $X$ is $\lambda$-paracompact.

(ii) Every directed open $\lambda$-cover has a locally finite open shrink.

(iii) Every directed open $\lambda$-cover has a locally finite closed refinement.
}

\begin{proof}
	 (i) $\Rightarrow$ (ii). Lemma \ref{le:3_1_1}.

(ii) $\Rightarrow$ (iii). Clear.

(iii) $\Rightarrow$ (i). Assume (iii) and let $\mathscr{U} = \{U_\alpha : \alpha < \lambda\}$ be a directed open $\sigma$-cover of $X$. Let $\mathscr{F} = \{F_\alpha : \alpha < \lambda\}$ be a LF closed refinement of $\mathscr{U}$ with $F_\alpha \subset U_\alpha$. For $s \in [\lambda]^{<\omega}$, let $V_s = X \setminus \bigcup_{\alpha \notin s} F_\alpha$. Then $\mathscr{V} = \{V_s : s \in [\lambda]^{<\omega}\}$ is a directed open $\sigma$-cover of $X$. Let $\mathscr{C} = \{C_s : s \in [\lambda]^{<\omega}\}$ be a LF closed refinement of $\mathscr{V}$ with $C_s \subset V_s$ for each $s \in [\lambda]^{<\omega}$. For $\alpha < \lambda$, let

$$G_\alpha = X \setminus \bigcup \{C_s : s \in [\lambda]^{<\omega}, C_s \cap F_\alpha = \phi\}$$

Then $F_\alpha \subset G_\alpha \subset U_\alpha$ for each $\alpha < \lambda$. It is easy to see that $\mathscr{G} = \{G_\alpha : \alpha < \lambda\}$ is a LF open refinement of $\mathscr{U}$, so $X$ is $\sigma$-paracompact. $\square$
\end{proof}

{\lemma{} \label{le:3_1_4}
An open cover $\mathscr{U}$ of $X$ has a $\sigma$-sequence of open refinements iff 
there exists a countable closed cover $F_n$ of $X$ such that 
for each $n \in \omega$, $\mathscr{U} \vert{} F_n$ has a point-finite open refinement 
in the subspace $F_n$.
}

\begin{proof}
	 ($\Rightarrow$). Suppose $\mathscr{U}$ has a $\theta$-sequence $\{\mathscr{V}_n = \{V_{n,\alpha} : \alpha \in \lambda\}\}_{n \in \omega}$ of open refinements. For $m, n < \omega$, let


$$F_{m,n} = \{x \in X : \vert{}\{n \in \omega : x \in \bigcup \mathscr{V}_n\}\vert{} \le m\}, \quad F_{m,-1} = \emptyset.$$

Then $\{F_{m,n}\}$ is a closed cover of $X$ such that for each $m, n < \omega$, $\mathscr{U} \vert{} F_{m,n}$ has a point-finite open refinement $\mathscr{V}_n \vert{} F_{m,n}$ in subspace $F_{m,n}$.

($\Leftarrow$). Suppose $\mathscr{U} = \{U_\alpha : \alpha \in \lambda\}$ is an open cover of $X$ and $\{F_n : n \in \omega\}$ is a countable closed cover of $X$ such that for each $n \in \omega$, $\mathscr{U} \vert{} F_n$ has a point-finite open refinement $\mathscr{V}_n = \{V_{n,\alpha} : \alpha \in \lambda\}$ in the subspace $F_n$ with $V_{n,\alpha} \subset U_\alpha$. Let $V_{n,\alpha} = V_{n,\alpha}' \cap F_n$ for some open subset $V_{n,\alpha}'$ of $X$. For $n \in \omega$, let


$$\mathscr{G}_n = \{V_{n,\alpha}' : \alpha \in \lambda\} \cup \{X \setminus \bigcup \{F_m : m \le n, \alpha \in \lambda\}\}.$$

Then $\{\mathscr{G}_n : n \in \omega\}$ is a $\sigma$-sequence of open refinements of $\mathscr{U}$. $\square$
\end{proof}

{\theorem{} $X$ is $\sigma$-submetacompact iff for every open $\sigma$-cover $\mathscr{U}$ of $X$ there is a countable closed cover $\{F_n\}$ of $X$ such that for each $n \in \omega$, $\mathscr{U} \vert{} F_n$ has a point-finite open refinement in the subspace $F_n$.
}


\subfile{ch3_2}
\end{document}
