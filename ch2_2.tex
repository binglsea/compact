\documentclass[main.tex]{subfiles}
%subfile : ch2_2.tex
\begin{document}
	\section{Paracompactness with Separation Axioms}  
	\label{sec:ch2_para_t}


... subparacompact, $\kappa$-shrinkable (every open $\kappa$-cover has an open $\kappa$-shrinkable refinement), normal, and $\kappa$-strongly screenable.

\begin{proof} 
Assume $X$ is fully normal. By Theorem 2.1.6, $X$ is $\kappa$-paracompact and $\kappa$-strongly screenable. By Lemma 1.1.7 and Theorem 2.1.6, $X$ is $\kappa$-subparacompact and $\kappa$-shrinkable, so $X$ is normal.
\end{proof}

 {\theorem{} \textup{(Michael [1953, 1957])}
For any $T_1$ regular space $X$, the following are equivalent:

\textup{(i)} $X$ is $\kappa$-paracompact.

\textup{(ii)}  Every open cover of $X$ has a $\sigma$-discrete open refinement.

\textup{(iii)} . Every open cover has a LF open refinement.

\textup{(iv)}  Every open cover has a $\sigma$-cp open refinement.
}
\begin{proof}
See Corollary 5.2.9. % TO_DO
\end{proof}

{\theorem{} \textup{(Michael [1959])} For any $T_1$ space $X$, the following are equivalent:

\textup{(i)}  $X$ is $\kappa$-paracompact.

\textup{(ii)}  Every open cover has a $\sigma$-cushioned open refinement.

\textup{(iii)}  Every open cover has a cushioned refinement.
}

\begin{proof}
See Corollary 5.2.9. % TO_DO
\end{proof}

\end{document}
